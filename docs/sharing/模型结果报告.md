# ThermoForge 模型结果报告

生成时间：2026-08-21 04:02 UTC-04　·　覆盖 13 个模型、18 个已注册版本

本报告由 `scripts/export_model_report.py` 从模型注册表 `models/` 与研究账本 `research/` 直接导出，公式与参数取自各模型包的 `artifact/`，指标取自包内 `metrics.json`（唯一实现 `research/metrics.py`，**比率制**，报告中转成百分数）。任何一个数字都可以回到对应的实验目录复核。

## 一、模型总览

| 模型 | 生产版本 | 状态 | 预测目标 | 输入数 | 形态 | R²（滚动） | CVRMSE（滚动） | NMBE（滚动） | MAPE（滚动） |
|---|---|---|---|---|---|---|---|---|---|
| `wx-chiller-power-ch01` | 1.0.0 | 生产 | power | 12 | 残差混合：Gordon-Ng 熵产模型 | 0.8059 | 4.85% | 2.47% | 3.80% |
| `wx-chiller-power-ch02` | 1.2.0 | 生产 | power | 15 | 纯梯度提升静态模型 | 0.7229 | 4.99% | -0.51% | 3.97% |
| `wx-chiller-power-ch03` | 1.0.0 | 生产 | power | 12 | 残差混合：Gordon-Ng 熵产模型 | -0.09844 | 7.19% | 3.40% | 5.56% |
| `wx-chiller-power-ch04` | 1.1.0 | 生产 | power | 15 | 残差混合：Gordon-Ng 熵产模型 | 0.6413 | 8.37% | -0.14% | 5.79% |
| `wx-chiller-power-static` | 1.0.0 | 生产 | power | 16 | 纯梯度提升静态模型 | 0.7001 | 5.28% | -0.40% | 4.18% |
| `wx-chwp-flow-total` | 1.0.0 | 生产 | flow_total | 9 | 纯梯度提升静态模型 | -0.9594 | 2.46% | 0.59% | 1.76% |
| `wx-ct-approach` | 1.0.0 | 生产 | approach | 5 | 二阶多项式 | 0.9586 | 4.15% | -1.00% | 3.46% |
| `wx-ct-fan-power-total` | 1.0.0 | 生产 | fan_power_total | 3 | 纯梯度提升静态模型 | 0.055 | 2.43% | -1.07% | 1.98% |
| `wx-ct-fan-power-unit` | 1.1.0 | 生产 | power | 7 | 频率幂律 + 湿空气密度修正 | 0.5774 | 6.08% | -0.22% | 4.82% |
| `wx-ct-supply-temp` | 1.0.0 | 生产 | t_ct_out | 11 | 线性基线 | 0.9776 | 1.27% | 0.24% | 0.97% |
| `wx-hx-heat-transfer` | 1.1.0 | 生产 | heat_transfer | 9 | 纯梯度提升静态模型 | 0.759 | 1.82% | 0.27% | 1.39% |
| `wx-hx-heat-transfer-static` | 1.0.0 | 生产 | heat_transfer | 9 | 纯梯度提升静态模型 | 0.759 | 1.82% | 0.27% | 1.39% |
| `wx-pump-power-unit` | 1.0.0 | 生产 | power | 1 | 逐台标定的三参数相似定律幂律 | 0.9898 | 1.86% | 0.15% | 0.99% |

> 表中指标一律取**滚动交叉验证**面（各目标声明的判据面），它是唯一用于发布决策的口径；验证集/测试集指标在各模型分节里给出。

## 二、口径说明（先读这一节，否则数字容易被误读）

### 指标定义

四个指标由 `research/metrics.py` 单一实现，模型代码不得自行计算：

```
RMSE   = sqrt( mean( (ŷ − y)² ) )
CVRMSE = RMSE / mean(y)                       变异系数化的 RMSE，相对误差幅度
NMBE   = mean(ŷ − y) / mean(y)                归一化平均偏差，**正=系统高估**
MAPE   = mean( |ŷ − y| / |y| )                丢弃 |y| < y_floor 的样本后计算
R²     = 1 − Σ(y − ŷ)² / Σ(y − mean(y))²      解释方差比例，可为负
```

- CVRMSE / NMBE / MAPE 在文件里存的是**比率**，本报告统一乘 100 显示为百分数。
- `mean(y) ≈ 0` 时 CVRMSE / NMBE 记为 `None` 而不是给一个看起来合理的数。
- MAPE 会丢掉 `|y| < y_floor` 的样本并记录有效比例，低于 0.8 会报 `TFX-905`。

### 三张判据面的区别

| 面 | 怎么来的 | 用途 |
|---|---|---|
| 滚动交叉验证 rolling_cv | 从训练区起，按固定跨度（7 或 30 天）逐步扩展训练窗、在下一窗上评估，取折间**均值** | **唯一的发布判据面** |
| 验证集 validate | 时间切分的中段（默认 15%） | 调参与诊断 |
| 测试集 A | 时间切分的末段（默认 15%），时间上离训练最远 | 最保守的泛化观察 |

切分按**时间**而非行数：边界对齐到采样周期整数倍，训练尾部 purge、验证头部 embargo（默认 2700 s = 45 min），重叠或间隙不足直接报 `TFX-903`。

### R² 为负是什么意思，为什么还能发布

R² 为负 = 该面上模型比「直接用这一折的均值」还差。它在**窄工况**上很容易出现：折内目标方差很小时分母趋零，一点点偏差就把 R² 拉到负数，而 CVRMSE 可以同时很好。
所以本项目的发布判据以 CVRMSE / NMBE 为主，R² 只在目标显式声明 `r2_min` 时才是硬门槛。报告里两个都给，看到 R² 负值请同时看 CVRMSE 和折间标准差。

### CVRMSE 跨模型不可直接比较

CVRMSE 的分母是各自的 `mean(y)`。群模型的均值是十几台之和，单台模型只有它的十几分之一——**同样的绝对误差，单台的 CVRMSE 会大十几倍**。研究账本 F-0087 用塔风机做过判决性分解：`wx-ct-fan-power-total`（群，2.43%）与 `wx-ct-fan-power-unit`（逐台，6.08%）看似差 2.5 倍，把逐台预测按时刻求和后在同一面上对比，逐台模型反而更好（测试面 3.10% vs 3.55%，验证面 4.46% vs 6.56%）。

### 白名单硬门禁（为什么输入列看起来「少了点」）

候选输入白名单会在目标层与实验层各拦一次：派生量不得用来预测它自己的原料。现场数据里 `load = current_percent × 9672 / 100`，用它预测功率是循环论证，能刷出 4.35% 的假 MAPE。报告里每个输入都标了 `数据来源`：`measured`（实测点位）/ `estimated`（由测点换算，如 lift、flow_ratio、湿球温度）。

### 复现方式

每个模型包自带 `environment.lock`（含全部依赖的版本与哈希）、`checksums.json`、以及 `golden.parquet` 预测集。实验在子进程里跑，线程数环境变量在 numpy/sklearn 导入**之前**设定；同机器 + 同环境锁必须逐位复现指标。包内不含任何 pickle。

## 三、逐模型明细

## 3.1　wx-chiller-power-ch01　—　CH01 静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | chiller_model.v1 |
| 建模对象 | CH01 |
| 预测目标 | power（kW） |
| 一句话说明 | CH01 静态模型（判据面 rolling_cv） |
| 研究目标 | RG-0012 · 冷机功耗静态模型（判据面=滚动交叉验证） |
| 假设 | H-0027：CH01：Gordon-Ng 打底 + XGBoost 残差（残差学习器可见两侧流量），滚动交叉验证折间均值 CVRMSE ≤ 0.12 |
| 实验 / 视图 | EXP-0048 / VIEW-0038 |
| 数据集修订 | `WX_CHILLER_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `8e3ded0d` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | cooling_load | 制冷量 | kW | float | estimated | 是 |
| 输入 | plr | 部分负荷率 | 1 | float | estimated | 是 |
| 输入 | t_evap_out | 蒸发器出水温（冷冻水供水） | Cel | float | measured | 是 |
| 输入 | t_cond_in | 冷凝器进水温（冷却水回水） | Cel | float | measured | 是 |
| 输入 | lift | 冷凝-蒸发温升 lift | K | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | hx_run_count | 板换运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | f_cw_branch | 本机冷却水支路流量 | m3/h | float | estimated | 是 |
| 输入 | f_chw_branch | 本机冷冻水支路流量 | m3/h | float | estimated | 是 |
| 输入 | cwp_frequency | 对应冷却泵频率 | Hz | float | measured | 是 |
| 输入 | chwp_frequency | 对应冷冻泵频率 | Hz | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：残差混合：Gordon-Ng 熵产模型（能量平衡 + 熵平衡 + 换热热阻，三参数） + XGBoost 残差修正

```
T_e = T_ei − Q_e · R_e                （蒸发温度，绝对温标 K）
K   = Q_e / T_e + ΔS_int              （总熵流率 kW/K）
Q_c = T_ci · K / (1 − R_c · K)        （冷凝排热 kW）
P   = Q_c − Q_e                       （压缩机功率 kW）

z_i = (x_i − mean_i) / std_i           （残差特征标准化，均值/标准差随包落盘）
P̂   = P + Σ_(t=1..400) g_t(z)      （g_t 为回归树，P 是上面的物理主干输出）
```

**代入本模型辨识出的参数**：

```
T_e = T_ei − 0.00253269 · Q_e
K   = Q_e / T_e + 0.465926
Q_c = T_ci · K / (1 − 1.269e-12 · K)
P   = Q_c − Q_e
P̂ = P + Σ_(t=1..400) g_t(z)
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| R_e | 蒸发器换热热阻 | 0.00253269 | K/kW |
| R_c | 冷凝器换热热阻 | 1.269e-12 | K/kW |
| ΔS_int | 内部熵产（不可逆损失） | 0.465926 | kW/K |
| n_estimators | 残差树棵数 | 400 | 1 |
| max_depth | 残差树最大深度 | 3 | 1 |
| learning_rate | 学习率 | 0.05 | 1 |
| subsample | 行采样比例 | 0.9 | 1 |
| colsample_bytree | 列采样比例 | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |

> 单调性天然满足：∂P/∂Q_e>0、∂P/∂T_ci>0、∂P/∂T_ei<0，无需额外施加约束。

> 辨识样本 7022 个；负荷下限 q_floor=1 kW（低于此值的样本不参与辨识）。

> **R_c 被辨识到 ≈0**：该台的换热热阻项在本工况区间内不可辨识（温差摆幅太小），物理主干退化为熵产项主导的近似恒定 COP 关系，误差主要由残差学习器承担——对照下方 physics_only 一行。

> 残差学习器可见 12 个标准化特征（顺序即包内 `preprocessing.yaml` 的 `feature_order`）；物理主干只用 Q_e / T_ei / T_ci 三列。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| lift | 冷凝-蒸发温升 lift | 37.6% |
| f_cw_branch | 本机冷却水支路流量 | 14.9% |
| t_cond_in | 冷凝器进水温（冷却水回水） | 12.7% |
| f_chw_branch | 本机冷冻水支路流量 | 10.5% |
| cooling_load | 制冷量 | 4.7% |
| cwp_frequency | 对应冷却泵频率 | 4.5% |
| chwp_frequency | 对应冷冻泵频率 | 3.8% |
| plr | 部分负荷率 | 3.6% |
| run_count | 运行台数 | 2.6% |
| t_evap_out | 蒸发器出水温（冷冻水供水） | 2.5% |
| ambient_t | 室外干球温度 | 1.4% |
| hx_run_count | 板换运行台数 | 1.2% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3646（8 折，跳过 0） | 0.8059 | 4.85% | 2.47% | 3.80% |
| 验证集（时间切分中段） | 2236 | 0.8672 | 8.17% | 4.16% | 6.15% |
| 测试集 A（时间切分末段） | 2185 | 0.4113 | 15.88% | 8.84% | 11.48% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.8059 | 0.1146 | 0.5367 | 0.9343 | 8 |
| CVRMSE | 4.85% | 1.91% | 3.24% | 9.58% | 8 |
| NMBE | 2.47% | 2.59% | -1.96% | 8.00% | 8 |
| MAPE | 3.80% | 1.62% | 2.10% | 7.84% | 8 |

**只用物理主干（不加残差修正）时的表现**，以及残差项占预测值的比重：

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE | 残差占比 |
|---|---|---|---|---|---|---|
| 验证集（时间切分中段） | 2236 | 0.802 | 9.98% | 6.05% | 7.96% | 6.93% |
| 测试集 A（时间切分末段） | 2185 | 0.3371 | 16.85% | 10.91% | 11.99% | 8.56% |

缺失值丢弃行数：训练 3420、验证 0、测试 54（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0012 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 12.00% | 4.85% | 通过 |
| \|NMBE\| ≤ | 3.50% | 2.47% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=19, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 21:00 | 10444 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 3600 s / embargo 2700 s | 7 |

视图哈希 `e2af6681572c1784…`；物理检查违规率 0.00%（0 次 / 2185 样本）。

## 3.2　wx-chiller-power-ch02　—　CH02 静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.2.0 |
| 状态 | 生产 |
| 对象模型 | chiller_model.v1 |
| 建模对象 | CH02 |
| 预测目标 | power（kW） |
| 一句话说明 | CH02 静态模型：纯 GBDT gbdt_static@v1 + 15 特征，rolling_cv R2=0.723 / CVRMSE=4.99% / NMBE=-0.51% |
| 研究目标 | RG-0012 · 冷机功耗静态模型（判据面=滚动交叉验证） |
| 假设 | H-0058：CH02：纯 GBDT（gbdt_static@v1，15 特征）在打开物理校验后指标不变，且预测满足硬物理约束，可进入发布。 |
| 实验 / 视图 | EXP-0085 / VIEW-0048 |
| 数据集修订 | `WX_CHILLER_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | cooling_load | 制冷量 | kW | float | estimated | 是 |
| 输入 | plr | 部分负荷率 | 1 | float | estimated | 是 |
| 输入 | t_evap_out | 蒸发器出水温（冷冻水供水） | Cel | float | measured | 是 |
| 输入 | t_cond_in | 冷凝器进水温（冷却水回水） | Cel | float | measured | 是 |
| 输入 | lift | 冷凝-蒸发温升 lift | K | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | hx_run_count | 板换运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | f_cw_branch | 本机冷却水支路流量 | m3/h | float | estimated | 是 |
| 输入 | f_chw_branch | 本机冷冻水支路流量 | m3/h | float | estimated | 是 |
| 输入 | cwp_frequency | 对应冷却泵频率 | Hz | float | measured | 是 |
| 输入 | chwp_frequency | 对应冷冻泵频率 | Hz | float | measured | 是 |
| 输入 | t_cw_tower_out | 冷却塔出水温 | Cel | float | measured | 是 |
| 输入 | t_chw_return | 冷冻水回水温 | Cel | float | measured | 是 |
| 输入 | ambient_h | 室外相对湿度 | % | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (cooling_load, plr, t_evap_out, t_cond_in, lift, run_count, hx_run_count, ambient_t, f_cw_branch, f_chw_branch, cwp_frequency, chwp_frequency, t_cw_tower_out, t_chw_return, ambient_h)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = cooling_load
        x_2 = plr
        x_3 = t_evap_out
        x_4 = t_cond_in
        x_5 = lift
        x_6 = run_count
        x_7 = hx_run_count
        x_8 = ambient_t
        x_9 = f_cw_branch
        x_10 = f_chw_branch
        x_11 = cwp_frequency
        x_12 = chwp_frequency
        x_13 = t_cw_tower_out
        x_14 = t_chw_return
        x_15 = ambient_h
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| t_cond_in | 冷凝器进水温（冷却水回水） | 49.4% |
| t_cw_tower_out | 冷却塔出水温 | 33.4% |
| plr | 部分负荷率 | 4.3% |
| cooling_load | 制冷量 | 4.2% |
| lift | 冷凝-蒸发温升 lift | 4.0% |
| t_chw_return | 冷冻水回水温 | 1.0% |
| chwp_frequency | 对应冷冻泵频率 | 0.8% |
| f_cw_branch | 本机冷却水支路流量 | 0.6% |
| run_count | 运行台数 | 0.5% |
| cwp_frequency | 对应冷却泵频率 | 0.4% |
| f_chw_branch | 本机冷冻水支路流量 | 0.4% |
| t_evap_out | 蒸发器出水温（冷冻水供水） | 0.3% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3739（8 折，跳过 0） | 0.7229 | 4.99% | -0.51% | 3.97% |
| 验证集（时间切分中段） | 364 | 0.6552 | 13.69% | 7.50% | 10.54% |
| 测试集 A（时间切分末段） | 6 | -15.97 | 35.22% | 31.83% | 31.92% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.7229 | 0.1234 | 0.5247 | 0.8919 | 8 |
| CVRMSE | 4.99% | 1.01% | 3.43% | 6.64% | 8 |
| NMBE | -0.51% | 2.09% | -3.99% | 3.65% | 8 |
| MAPE | 3.97% | 0.82% | 2.81% | 5.07% | 8 |

缺失值丢弃行数：训练 2766、验证 1872、测试 2233（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0012 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 12.00% | 4.99% | 通过 |
| \|NMBE\| ≤ | 3.50% | 0.51% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=23, max_rel_error=0） |
| rollback_recorded | 通过 | 回滚目标: 1.0.0 |

回滚目标版本：`1.0.0`。

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 22:00 | 10448 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `c2c9bf032d19c66c…`；物理检查违规率 0.00%（0 次 / 6 样本）。

### 版本历史

| 版本 | 状态 | 实验 | 形态 | R²(滚动) | CVRMSE(滚动) | NMBE(滚动) | 说明 |
|---|---|---|---|---|---|---|---|
| 1.0.0 | 已弃用 | EXP-0049 | 残差混合：Gordon-Ng 熵产模型 | 0.5878 | 5.79% | -1.43% | CH02 静态模型（判据面 rolling_cv） |
| 1.1.0 | 候选 | EXP-0072 | 纯梯度提升静态模型 | 0.7229 | 4.99% | -0.51% | CH02 静态模型：纯 GBDT（lab gbdt_static@v1）+15  |
| 1.2.0 | 生产（当前生产） | EXP-0085 | 纯梯度提升静态模型 | 0.7229 | 4.99% | -0.51% | CH02 静态模型：纯 GBDT gbdt_static@v1 + 15 特征， |

## 3.3　wx-chiller-power-ch03　—　CH03 静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | chiller_model.v1 |
| 建模对象 | CH03 |
| 预测目标 | power（kW） |
| 一句话说明 | CH03 静态模型（判据面 rolling_cv） |
| 研究目标 | RG-0012 · 冷机功耗静态模型（判据面=滚动交叉验证） |
| 假设 | H-0029：CH03：Gordon-Ng 打底 + XGBoost 残差（残差学习器可见两侧流量），滚动交叉验证折间均值 CVRMSE ≤ 0.12 |
| 实验 / 视图 | EXP-0050 / VIEW-0040 |
| 数据集修订 | `WX_CHILLER_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `8e3ded0d` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | cooling_load | 制冷量 | kW | float | estimated | 是 |
| 输入 | plr | 部分负荷率 | 1 | float | estimated | 是 |
| 输入 | t_evap_out | 蒸发器出水温（冷冻水供水） | Cel | float | measured | 是 |
| 输入 | t_cond_in | 冷凝器进水温（冷却水回水） | Cel | float | measured | 是 |
| 输入 | lift | 冷凝-蒸发温升 lift | K | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | hx_run_count | 板换运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | f_cw_branch | 本机冷却水支路流量 | m3/h | float | estimated | 是 |
| 输入 | f_chw_branch | 本机冷冻水支路流量 | m3/h | float | estimated | 是 |
| 输入 | cwp_frequency | 对应冷却泵频率 | Hz | float | measured | 是 |
| 输入 | chwp_frequency | 对应冷冻泵频率 | Hz | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：残差混合：Gordon-Ng 熵产模型（能量平衡 + 熵平衡 + 换热热阻，三参数） + XGBoost 残差修正

```
T_e = T_ei − Q_e · R_e                （蒸发温度，绝对温标 K）
K   = Q_e / T_e + ΔS_int              （总熵流率 kW/K）
Q_c = T_ci · K / (1 − R_c · K)        （冷凝排热 kW）
P   = Q_c − Q_e                       （压缩机功率 kW）

z_i = (x_i − mean_i) / std_i           （残差特征标准化，均值/标准差随包落盘）
P̂   = P + Σ_(t=1..400) g_t(z)      （g_t 为回归树，P 是上面的物理主干输出）
```

**代入本模型辨识出的参数**：

```
T_e = T_ei − 2.914e-14 · Q_e
K   = Q_e / T_e + 0.951949
Q_c = T_ci · K / (1 − 0.00108726 · K)
P   = Q_c − Q_e
P̂ = P + Σ_(t=1..400) g_t(z)
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| R_e | 蒸发器换热热阻 | 2.914e-14 | K/kW |
| R_c | 冷凝器换热热阻 | 0.00108726 | K/kW |
| ΔS_int | 内部熵产（不可逆损失） | 0.951949 | kW/K |
| n_estimators | 残差树棵数 | 400 | 1 |
| max_depth | 残差树最大深度 | 3 | 1 |
| learning_rate | 学习率 | 0.05 | 1 |
| subsample | 行采样比例 | 0.9 | 1 |
| colsample_bytree | 列采样比例 | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |

> 单调性天然满足：∂P/∂Q_e>0、∂P/∂T_ci>0、∂P/∂T_ei<0，无需额外施加约束。

> 辨识样本 6826 个；负荷下限 q_floor=1 kW（低于此值的样本不参与辨识）。

> **R_e 被辨识到 ≈0**：该台的换热热阻项在本工况区间内不可辨识（温差摆幅太小），物理主干退化为熵产项主导的近似恒定 COP 关系，误差主要由残差学习器承担——对照下方 physics_only 一行。

> 残差学习器可见 12 个标准化特征（顺序即包内 `preprocessing.yaml` 的 `feature_order`）；物理主干只用 Q_e / T_ei / T_ci 三列。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| lift | 冷凝-蒸发温升 lift | 43.5% |
| t_cond_in | 冷凝器进水温（冷却水回水） | 28.3% |
| cooling_load | 制冷量 | 4.9% |
| f_chw_branch | 本机冷冻水支路流量 | 4.3% |
| plr | 部分负荷率 | 3.6% |
| hx_run_count | 板换运行台数 | 3.2% |
| f_cw_branch | 本机冷却水支路流量 | 3.2% |
| cwp_frequency | 对应冷却泵频率 | 2.5% |
| run_count | 运行台数 | 1.9% |
| t_evap_out | 蒸发器出水温（冷冻水供水） | 1.6% |
| chwp_frequency | 对应冷冻泵频率 | 1.5% |
| ambient_t | 室外干球温度 | 1.5% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3732（8 折，跳过 0） | -0.09844 | 7.19% | 3.40% | 5.56% |
| 验证集（时间切分中段） | 2236 | 0.8077 | 11.55% | 1.68% | 9.70% |
| 测试集 A（时间切分末段） | 1949 | 0.2866 | 18.67% | 7.27% | 12.43% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | -0.09844 | 1.954 | -5.212 | 0.8883 | 8 |
| CVRMSE | 7.19% | 1.26% | 4.59% | 8.49% | 8 |
| NMBE | 3.40% | 2.32% | 0.54% | 7.35% | 8 |
| MAPE | 5.56% | 1.27% | 3.80% | 7.67% | 8 |

**只用物理主干（不加残差修正）时的表现**，以及残差项占预测值的比重：

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE | 残差占比 |
|---|---|---|---|---|---|---|
| 验证集（时间切分中段） | 2236 | 0.6305 | 16.01% | -4.18% | 15.45% | 13.56% |
| 测试集 A（时间切分末段） | 1949 | -0.08228 | 23.00% | -16.56% | 21.93% | 27.03% |

缺失值丢弃行数：训练 3515、验证 0、测试 290（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0012 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 12.00% | 7.19% | 通过 |
| \|NMBE\| ≤ | 3.50% | 3.40% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=21, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 21:00 | 10444 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 3600 s / embargo 2700 s | 7 |

视图哈希 `86a3367684cdd2fe…`；物理检查违规率 0.00%（0 次 / 1949 样本）。

## 3.4　wx-chiller-power-ch04　—　CH04 静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.1.0 |
| 状态 | 生产 |
| 对象模型 | chiller_model.v1 |
| 建模对象 | CH04 |
| 预测目标 | power（kW） |
| 一句话说明 | CH04 静态模型：hybrid Gordon-Ng + XGBoost 残差 + 15 特征，rolling_cv R2=0.641 / CVRMSE=8.37% / NMBE=-0.14% |
| 研究目标 | RG-0012 · 冷机功耗静态模型（判据面=滚动交叉验证） |
| 假设 | H-0049：CH04：runtime_accum 不得作为自变量（累计运行时长是时间代理，what-if 用途下不可操纵且必然外推出训练区间）。从 EXP-0056 的16 特征里摘掉它、其余不变，复核当时的 NMBE 改善是否依赖它。 |
| 实验 / 视图 | EXP-0070 / VIEW-0050 |
| 数据集修订 | `WX_CHILLER_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `0b7c1dc5` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | cooling_load | 制冷量 | kW | float | estimated | 是 |
| 输入 | plr | 部分负荷率 | 1 | float | estimated | 是 |
| 输入 | t_evap_out | 蒸发器出水温（冷冻水供水） | Cel | float | measured | 是 |
| 输入 | t_cond_in | 冷凝器进水温（冷却水回水） | Cel | float | measured | 是 |
| 输入 | lift | 冷凝-蒸发温升 lift | K | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | hx_run_count | 板换运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | f_cw_branch | 本机冷却水支路流量 | m3/h | float | estimated | 是 |
| 输入 | f_chw_branch | 本机冷冻水支路流量 | m3/h | float | estimated | 是 |
| 输入 | cwp_frequency | 对应冷却泵频率 | Hz | float | measured | 是 |
| 输入 | chwp_frequency | 对应冷冻泵频率 | Hz | float | measured | 是 |
| 输入 | t_cw_tower_out | 冷却塔出水温 | Cel | float | measured | 是 |
| 输入 | t_chw_return | 冷冻水回水温 | Cel | float | measured | 是 |
| 输入 | ambient_h | 室外相对湿度 | % | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：残差混合：Gordon-Ng 熵产模型（能量平衡 + 熵平衡 + 换热热阻，三参数） + XGBoost 残差修正

```
T_e = T_ei − Q_e · R_e                （蒸发温度，绝对温标 K）
K   = Q_e / T_e + ΔS_int              （总熵流率 kW/K）
Q_c = T_ci · K / (1 − R_c · K)        （冷凝排热 kW）
P   = Q_c − Q_e                       （压缩机功率 kW）

z_i = (x_i − mean_i) / std_i           （残差特征标准化，均值/标准差随包落盘）
P̂   = P + Σ_(t=1..400) g_t(z)      （g_t 为回归树，P 是上面的物理主干输出）
```

**代入本模型辨识出的参数**：

```
T_e = T_ei − 2.526e-17 · Q_e
K   = Q_e / T_e + 1.45822
Q_c = T_ci · K / (1 − 9.052e-16 · K)
P   = Q_c − Q_e
P̂ = P + Σ_(t=1..400) g_t(z)
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| R_e | 蒸发器换热热阻 | 2.526e-17 | K/kW |
| R_c | 冷凝器换热热阻 | 9.052e-16 | K/kW |
| ΔS_int | 内部熵产（不可逆损失） | 1.45822 | kW/K |
| n_estimators | 残差树棵数 | 400 | 1 |
| max_depth | 残差树最大深度 | 3 | 1 |
| learning_rate | 学习率 | 0.05 | 1 |
| subsample | 行采样比例 | 0.9 | 1 |
| colsample_bytree | 列采样比例 | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |

> 单调性天然满足：∂P/∂Q_e>0、∂P/∂T_ci>0、∂P/∂T_ei<0，无需额外施加约束。

> 辨识样本 8286 个；负荷下限 q_floor=1 kW（低于此值的样本不参与辨识）。

> **R_e、R_c 被辨识到 ≈0**：该台的换热热阻项在本工况区间内不可辨识（温差摆幅太小），物理主干退化为熵产项主导的近似恒定 COP 关系，误差主要由残差学习器承担——对照下方 physics_only 一行。

> 残差学习器可见 15 个标准化特征（顺序即包内 `preprocessing.yaml` 的 `feature_order`）；物理主干只用 Q_e / T_ei / T_ci 三列。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| t_cond_in | 冷凝器进水温（冷却水回水） | 42.3% |
| lift | 冷凝-蒸发温升 lift | 18.2% |
| t_cw_tower_out | 冷却塔出水温 | 8.6% |
| cooling_load | 制冷量 | 5.1% |
| cwp_frequency | 对应冷却泵频率 | 4.9% |
| f_chw_branch | 本机冷冻水支路流量 | 3.3% |
| plr | 部分负荷率 | 3.3% |
| t_chw_return | 冷冻水回水温 | 3.1% |
| run_count | 运行台数 | 2.8% |
| ambient_h | 室外相对湿度 | 2.4% |
| f_cw_branch | 本机冷却水支路流量 | 2.0% |
| chwp_frequency | 对应冷冻泵频率 | 1.9% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 4941（8 折，跳过 0） | 0.6413 | 8.37% | -0.14% | 5.79% |
| 验证集（时间切分中段） | 1873 | 0.8486 | 9.38% | -1.33% | 7.64% |
| 测试集 A（时间切分末段） | 2182 | 0.1539 | 17.74% | -3.30% | 14.38% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.6413 | 0.06209 | 0.5412 | 0.7193 | 8 |
| CVRMSE | 8.37% | 2.18% | 6.00% | 12.92% | 8 |
| NMBE | -0.14% | 3.02% | -6.13% | 4.99% | 8 |
| MAPE | 5.79% | 1.62% | 3.88% | 9.46% | 8 |

**只用物理主干（不加残差修正）时的表现**，以及残差项占预测值的比重：

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE | 残差占比 |
|---|---|---|---|---|---|---|
| 验证集（时间切分中段） | 1873 | 0.3961 | 18.74% | -7.02% | 18.17% | 15.08% |
| 测试集 A（时间切分末段） | 2182 | -5.113 | 47.69% | -44.62% | 49.09% | 41.48% |

缺失值丢弃行数：训练 2151、验证 363、测试 57（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0012 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 12.00% | 8.37% | 通过 |
| \|NMBE\| ≤ | 3.50% | 0.14% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=25, max_rel_error=0） |
| rollback_recorded | 通过 | 回滚目标: 1.0.0 |

回滚目标版本：`1.0.0`。

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 22:00 | 10448 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `bc8d15ac535cb4d7…`；物理检查违规率 0.00%（0 次 / 2182 样本）。

### 版本历史

| 版本 | 状态 | 实验 | 形态 | R²(滚动) | CVRMSE(滚动) | NMBE(滚动) | 说明 |
|---|---|---|---|---|---|---|---|
| 1.0.0 | 已弃用 | EXP-0051 | 残差混合：Gordon-Ng 熵产模型 | 0.5796 | 9.03% | 0.22% | CH04 静态模型（判据面 rolling_cv） |
| 1.1.0 | 生产（当前生产） | EXP-0070 | 残差混合：Gordon-Ng 熵产模型 | 0.6413 | 8.37% | -0.14% | CH04 静态模型：hybrid Gordon-Ng + XGBoost 残差  |

## 3.5　wx-chiller-power-static　—　冷机功耗静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | chiller_model.v1 |
| 建模对象 | CH02 |
| 预测目标 | power（kW） |
| 一句话说明 | 冷机功耗静态模型（what-if 用途）；EXP-0058 的干净重跑，原实验受实验室目标列泄漏影响，指标不可用 |
| 研究目标 | RG-0012 · 冷机功耗静态模型（判据面=滚动交叉验证） |
| 假设 | H-0037：CH02：去掉物理主干、直接用梯度提升在白名单特征上回归（实验室模块 gbdt_static@v1），应优于 hybrid —— 蒸发侧温差摆幅仅 0.4 K 使热阻不可辨识（手册 §7.11），主干只贡献近似常数偏移，残差学习器还得先学回来。 |
| 实验 / 视图 | EXP-0176 / VIEW-0044 |
| 数据集修订 | `WX_CHILLER_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | cooling_load | 制冷量 | kW | float | estimated | 是 |
| 输入 | plr | 部分负荷率 | 1 | float | estimated | 是 |
| 输入 | t_evap_out | 蒸发器出水温（冷冻水供水） | Cel | float | measured | 是 |
| 输入 | t_cond_in | 冷凝器进水温（冷却水回水） | Cel | float | measured | 是 |
| 输入 | lift | 冷凝-蒸发温升 lift | K | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | hx_run_count | 板换运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | f_cw_branch | 本机冷却水支路流量 | m3/h | float | estimated | 是 |
| 输入 | f_chw_branch | 本机冷冻水支路流量 | m3/h | float | estimated | 是 |
| 输入 | cwp_frequency | 对应冷却泵频率 | Hz | float | measured | 是 |
| 输入 | chwp_frequency | 对应冷冻泵频率 | Hz | float | measured | 是 |
| 输入 | t_cw_tower_out | 冷却塔出水温 | Cel | float | measured | 是 |
| 输入 | t_chw_return | 冷冻水回水温 | Cel | float | measured | 是 |
| 输入 | ambient_h | 室外相对湿度 | % | float | measured | 是 |
| 输入 | runtime_accum | 累计运行时长 | s | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (cooling_load, plr, t_evap_out, t_cond_in, lift, run_count, hx_run_count, ambient_t, f_cw_branch, f_chw_branch, cwp_frequency, chwp_frequency, t_cw_tower_out, t_chw_return, ambient_h, runtime_accum)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = cooling_load
        x_2 = plr
        x_3 = t_evap_out
        x_4 = t_cond_in
        x_5 = lift
        x_6 = run_count
        x_7 = hx_run_count
        x_8 = ambient_t
        x_9 = f_cw_branch
        x_10 = f_chw_branch
        x_11 = cwp_frequency
        x_12 = chwp_frequency
        x_13 = t_cw_tower_out
        x_14 = t_chw_return
        x_15 = ambient_h
        x_16 = runtime_accum
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| t_cond_in | 冷凝器进水温（冷却水回水） | 51.4% |
| t_cw_tower_out | 冷却塔出水温 | 34.5% |
| cooling_load | 制冷量 | 4.6% |
| lift | 冷凝-蒸发温升 lift | 3.8% |
| t_chw_return | 冷冻水回水温 | 1.0% |
| plr | 部分负荷率 | 0.8% |
| chwp_frequency | 对应冷冻泵频率 | 0.7% |
| runtime_accum | 累计运行时长 | 0.6% |
| f_cw_branch | 本机冷却水支路流量 | 0.5% |
| cwp_frequency | 对应冷却泵频率 | 0.4% |
| f_chw_branch | 本机冷冻水支路流量 | 0.3% |
| hx_run_count | 板换运行台数 | 0.3% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3739（8 折，跳过 0） | 0.7001 | 5.28% | -0.40% | 4.18% |
| 验证集（时间切分中段） | 364 | 0.748 | 11.70% | -0.34% | 10.48% |
| 测试集 A（时间切分末段） | 6 | -8.95 | 26.97% | 22.29% | 22.40% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.7001 | 0.1042 | 0.5869 | 0.8662 | 8 |
| CVRMSE | 5.28% | 0.97% | 3.90% | 6.69% | 8 |
| NMBE | -0.40% | 2.63% | -4.59% | 3.87% | 8 |
| MAPE | 4.18% | 0.86% | 3.12% | 5.50% | 8 |

缺失值丢弃行数：训练 2766、验证 1872、测试 2233（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0012 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 12.00% | 5.28% | 通过 |
| \|NMBE\| ≤ | 3.50% | 0.40% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=24, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 22:00 | 10448 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `226cb5c42188a3f5…`；物理检查违规率 0.00%（0 次 / 6 样本）。

## 3.6　wx-chwp-flow-total　—　冷冻水泵群总流量

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | pump_model.v1 |
| 建模对象 | CHWPBANK |
| 预测目标 | flow_total（m3/h） |
| 一句话说明 | 冷冻水泵群总流量 |
| 研究目标 | RG-0020 · 冷冻水泵群总流量模型（判据面=滚动交叉验证，R²≥0.90） |
| 假设 | H-0104：用已验证的静态 GBDT 实验室模型 gbdt_static@v1 对 VIEW-0059 全部九维候选输入建模，验证其能否在 30 天滚动判据面上把 CVRMSE 从 EXP-0130 的 2.81% 压到约 2.4% 以内，从而满足 R²≥0.90 且 \|NMBE\|≤0.02。 |
| 实验 / 视图 | EXP-0131 / VIEW-0059 |
| 数据集修订 | `WX_CHWP_MODEL@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | power_total | 水泵群总功耗 | kW | float | measured | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | freq_mean | 水泵平均频率 | Hz | float | measured | 是 |
| 输入 | freq_max | 水泵最高频率 | Hz | float | measured | 是 |
| 输入 | t_supply | 供水温度 | Cel | float | measured | 是 |
| 输入 | t_return | 回水温度 | Cel | float | measured | 是 |
| 输入 | delta_t | 供回水温差 | K | float | estimated | 是 |
| 输入 | chiller_run_count | 冷机运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| **输出** | flow_total | 水泵群总流量 | m3/h | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (power_total, run_count, freq_mean, freq_max, t_supply, t_return, delta_t, chiller_run_count, ambient_t)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = power_total
        x_2 = run_count
        x_3 = freq_mean
        x_4 = freq_max
        x_5 = t_supply
        x_6 = t_return
        x_7 = delta_t
        x_8 = chiller_run_count
        x_9 = ambient_t
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260808 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| power_total | 水泵群总功耗 | 61.8% |
| freq_mean | 水泵平均频率 | 21.7% |
| freq_max | 水泵最高频率 | 5.2% |
| run_count | 运行台数 | 5.1% |
| chiller_run_count | 冷机运行台数 | 3.8% |
| t_supply | 供水温度 | 0.8% |
| delta_t | 供回水温差 | 0.8% |
| ambient_t | 室外干球温度 | 0.6% |
| t_return | 回水温度 | 0.2% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 22896（8 折，跳过 0） | -0.9594 | 2.46% | 0.59% | 1.76% |
| 验证集（时间切分中段） | 4908 | 0.9274 | 2.38% | 0.12% | 1.81% |
| 测试集 A（时间切分末段） | 4913 | 0.8871 | 1.94% | 0.82% | 1.41% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | -0.9594 | 1.932 | -4.68 | 0.9708 | 8 |
| CVRMSE | 2.46% | 0.84% | 1.39% | 4.13% | 8 |
| NMBE | 0.59% | 1.35% | -1.86% | 2.75% | 8 |
| MAPE | 1.76% | 0.69% | 0.87% | 3.17% | 8 |
| RMSE | 75.3 | 27.08 | 37.82 | 128.3 | 8 |
| MAE | 54.06 | 22.36 | 27.11 | 100 | 8 |

缺失值丢弃行数：训练 2、验证 1、测试 0（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0020 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 3.50% | 2.46% | 通过 |
| \|NMBE\| ≤ | 2.00% | 0.59% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=16, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-09-15 06:45 | 22922 |
| 验证 validate | 2025-09-15 07:30 → 2025-11-05 10:45 | 4909 |
| 测试 A | 2025-11-05 10:45 → 2025-12-26 15:30 | 4913 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `1ca2fa98de1adf45…`；物理检查违规率 0.00%（0 次 / 4913 样本）。

## 3.7　wx-ct-approach　—　冷却塔逼近度模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | ct_model.v1 |
| 建模对象 | CTBANK |
| 预测目标 | approach（K） |
| 一句话说明 | 冷却塔逼近度模型（制冷季）：Merkel/NTU 主干 + 偏差校正（lab approach_bias_correction_v1@v1），rolling_cv 4 折 R2=0.959 / CVRMSE=4.15% / NMBE=-1.00% |
| 研究目标 | RG-0019 · 冷却塔逼近度模型·制冷季（判据面=滚动交叉验证，R²≥0.90） |
| 假设 | H-0099：在 VIEW-0058 的 Merkel/NTU 主干不可解释的残差上，加入以湿球温度与冷却水流量为主的多项式偏置校正项，验证能否将 \|NMBE\| 从 0.047 压到 ≤0.02 且 R² 不低于 0.90。 |
| 实验 / 视图 | EXP-0126 / VIEW-0058 |
| 数据集修订 | `WX_CT_SEASON@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | t_ct_in | 冷却塔群进水温 | Cel | float | measured | 是 |
| 输入 | f_cw_total | 冷却水总流量 | m3/h | float | measured | 是 |
| 输入 | t_wetbulb | 室外湿球温度 | Cel | float | estimated | 是 |
| 输入 | fan_freq_mean | 风机平均频率 | Hz | float | measured | 是 |
| 输入 | fan_run_count | 风机运行台数 | 1 | float | measured | 是 |
| **输出** | approach | 逼近度（出水温 − 湿球温） | K | float | estimated | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：二阶多项式（含全部两两交互）岭回归 —— 实验室模块 approach_bias_correction_v1@v1

```
d(x) = [1, x_1…x_5, x_i·x_j (1 ≤ i ≤ j ≤ 5)]      → 1 + 5 + 15 = 21 项
z_k  = (d_k − μ_k) / (σ_k + 1e−9)                  （k = 1…20，常数项不缩放）
approach = θ_0 + Σ_(k=1..20) θ_k · z_k

拟合：(ZᵀZ + λI)·θ = Zᵀy，λ = 1e−3     （闭式岭回归，无迭代、无随机性）
```

**代入本模型辨识出的参数**：

```
x = (t_ct_in, f_cw_total, t_wetbulb, fan_freq_mean, fan_run_count)
θ_0 = 5.37239 K
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| θ_0 | 常数项（不参与标准化） | 5.37239 | K |
| θ_1 | t_ct_in 项系数（标准化后） | 3.16983 | K |
| μ,σ_1 | t_ct_in 训练集均值 / 标准差 | 30.316 / 2.90665 | — |
| θ_2 | f_cw_total 项系数（标准化后） | 1.21917 | K |
| μ,σ_2 | f_cw_total 训练集均值 / 标准差 | 2780.26 / 371.37 | — |
| θ_3 | t_wetbulb 项系数（标准化后） | -2.98557 | K |
| μ,σ_3 | t_wetbulb 训练集均值 / 标准差 | 18.8968 / 3.85248 | — |
| θ_4 | fan_freq_mean 项系数（标准化后） | -0.852082 | K |
| μ,σ_4 | fan_freq_mean 训练集均值 / 标准差 | 47.3067 / 1.48243 | — |
| θ_5 | fan_run_count 项系数（标准化后） | -0.509314 | K |
| μ,σ_5 | fan_run_count 训练集均值 / 标准差 | 9.05466 / 0.412771 | — |
| θ_6 | t_ct_in² 项系数（标准化后） | 0.0165947 | K |
| μ,σ_6 | t_ct_in² 训练集均值 / 标准差 | 927.506 / 171.05 | — |
| θ_7 | t_ct_in · f_cw_total 项系数（标准化后） | 2.33849 | K |
| μ,σ_7 | t_ct_in · f_cw_total 训练集均值 / 标准差 | 84597.5 / 15092.8 | — |
| θ_8 | t_ct_in · t_wetbulb 项系数（标准化后） | -0.933729 | K |
| μ,σ_8 | t_ct_in · t_wetbulb 训练集均值 / 标准差 | 583.058 / 163.755 | — |
| θ_9 | t_ct_in · fan_freq_mean 项系数（标准化后） | -0.274162 | K |
| μ,σ_9 | t_ct_in · fan_freq_mean 训练集均值 / 标准差 | 1434.81 / 149.952 | — |
| θ_10 | t_ct_in · fan_run_count 项系数（标准化后） | -1.35526 | K |
| μ,σ_10 | t_ct_in · fan_run_count 训练集均值 / 标准差 | 274.426 / 28.3496 | — |
| θ_11 | f_cw_total² 项系数（标准化后） | -0.504338 | K |
| μ,σ_11 | f_cw_total² 训练集均值 / 标准差 | 7.86777e+06 / 2.03594e+06 | — |
| θ_12 | f_cw_total · t_wetbulb 项系数（标准化后） | -1.90975 | K |
| μ,σ_12 | f_cw_total · t_wetbulb 训练集均值 / 标准差 | 53308.6 / 14674.7 | — |
| θ_13 | f_cw_total · fan_freq_mean 项系数（标准化后） | -0.337294 | K |
| μ,σ_13 | f_cw_total · fan_freq_mean 训练集均值 / 标准差 | 131710 / 18848.3 | — |
| θ_14 | f_cw_total · fan_run_count 项系数（标准化后） | -0.44652 | K |
| μ,σ_14 | f_cw_total · fan_run_count 训练集均值 / 标准差 | 25171.4 / 3540.16 | — |
| θ_15 | t_wetbulb² 项系数（标准化后） | 0.60814 | K |
| μ,σ_15 | t_wetbulb² 训练集均值 / 标准差 | 371.93 / 139.009 | — |
| θ_16 | t_wetbulb · fan_freq_mean 项系数（标准化后） | -0.75332 | K |
| μ,σ_16 | t_wetbulb · fan_freq_mean 训练集均值 / 标准差 | 895.827 / 189.459 | — |
| θ_17 | t_wetbulb · fan_run_count 项系数（标准化后） | 1.36582 | K |
| μ,σ_17 | t_wetbulb · fan_run_count 训练集均值 / 标准差 | 171.05 / 35.511 | — |
| θ_18 | fan_freq_mean² 项系数（标准化后） | 0.275295 | K |
| μ,σ_18 | fan_freq_mean² 训练集均值 / 标准差 | 2240.12 / 126.392 | — |
| θ_19 | fan_freq_mean · fan_run_count 项系数（标准化后） | 1.29301 | K |
| μ,σ_19 | fan_freq_mean · fan_run_count 训练集均值 / 标准差 | 428.356 / 23.908 | — |
| θ_20 | fan_run_count² 项系数（标准化后） | -0.120379 | K |
| μ,σ_20 | fan_run_count² 训练集均值 / 标准差 | 82.1573 / 8.62979 | — |

> **命名与实现不一致**：模型包 `description` 写的是「Merkel/NTU 主干 + 偏差校正」（沿用假设 H-0099 的措辞），但落盘的 `lab_source.py` 里没有 Merkel 主干——实际形态就是上面这个 21 项二阶多项式岭回归。以本节公式为准。

> 逼近度 approach = t_ct_out − t_wetbulb 是派生量；预测它再加上湿球温度即得冷却塔出水温。

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 10434（4 折，跳过 0） | 0.9586 | 4.15% | -1.00% | 3.46% |
| 验证集（时间切分中段） | 2236 | 0.9751 | 4.00% | -2.49% | 3.29% |
| 测试集 A（时间切分末段） | 2238 | 0.9317 | 5.86% | -4.97% | 5.59% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.9586 | 0.01108 | 0.9395 | 0.9661 | 4 |
| CVRMSE | 4.15% | 0.26% | 3.73% | 4.43% | 4 |
| NMBE | -1.00% | 2.16% | -3.16% | 1.91% | 4 |
| MAPE | 3.46% | 0.45% | 2.80% | 3.98% | 4 |
| RMSE | 0.2446 | 0.0502 | 0.1893 | 0.2977 | 4 |
| MAE | 0.1956 | 0.04797 | 0.1469 | 0.2446 | 4 |

缺失值丢弃行数：训练 2、验证 0、测试 1（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0019 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| \|NMBE\| ≤ | 2.00% | 1.00% | 通过 |
| R² ≥ | 0.9 | 0.9586 | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=11, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2025-04-11 02:30 → 2025-08-27 22:00 | 10448 |
| 验证 validate | 2025-08-27 22:45 → 2025-09-20 06:00 | 2236 |
| 测试 A | 2025-09-20 06:00 → 2025-10-16 21:30 | 2239 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `0ae42afa6f689e60…`；物理检查违规率 0.00%（0 次 / 2238 样本）。

## 3.8　wx-ct-fan-power-total　—　冷却塔风机群总功耗

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | ct_model.v1 |
| 建模对象 | CTBANK |
| 预测目标 | fan_power_total（kW） |
| 一句话说明 | 冷却塔风机群总功耗 |
| 研究目标 | RG-0017 · 冷却塔风机总功耗模型（判据面=滚动交叉验证，R²≥0.90） |
| 假设 | H-0065：总风机功耗由运行台数与单台频率三次方非线性交互决定，纯静态 GBDT（lab gbdt_static@v1）可表达该非线性与台数分段，验证其能否把 rolling_cv R² 从线性 ridge 的 0.295 提升到 0.90 门槛。 |
| 实验 / 视图 | EXP-0092 / VIEW-0054 |
| 数据集修订 | `WX_CT_MODEL@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | fan_freq_mean | 风机平均频率 | Hz | float | measured | 是 |
| 输入 | fan_freq_max | 风机最高频率 | Hz | float | measured | 是 |
| 输入 | fan_run_count | 风机运行台数 | 1 | float | measured | 是 |
| **输出** | fan_power_total | 风机群总功耗 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (fan_freq_mean, fan_freq_max, fan_run_count)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = fan_freq_mean
        x_2 = fan_freq_max
        x_3 = fan_run_count
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260808 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| fan_freq_mean | 风机平均频率 | 67.5% |
| fan_freq_max | 风机最高频率 | 26.7% |
| fan_run_count | 风机运行台数 | 5.8% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 8027（12 折，跳过 0） | 0.055 | 2.43% | -1.07% | 1.98% |
| 验证集（时间切分中段） | 4960 | 0.9326 | 6.56% | 4.59% | 4.32% |
| 测试集 A（时间切分末段） | 4963 | 0.9908 | 3.55% | -0.68% | 2.43% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.055 | 1.507 | -3.917 | 0.9894 | 12 |
| CVRMSE | 2.43% | 0.85% | 1.16% | 4.53% | 12 |
| NMBE | -1.07% | 1.22% | -3.47% | 1.04% | 12 |
| MAPE | 1.98% | 0.69% | 0.88% | 3.73% | 12 |
| RMSE | 7.146 | 2.486 | 3.548 | 13.38 | 12 |
| MAE | 5.678 | 2.007 | 2.704 | 10.79 | 12 |

缺失值丢弃行数：训练 0、验证 0、测试 0（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0017 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 5.00% | 2.43% | 通过 |
| \|NMBE\| ≤ | 2.00% | 1.07% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=6, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-09-17 18:00 | 23159 |
| 验证 validate | 2025-09-17 18:45 → 2025-11-08 10:45 | 4960 |
| 测试 A | 2025-11-08 10:45 → 2025-12-30 21:15 | 4963 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `1ad5d7a6990474e6…`；物理检查违规率 0.00%（0 次 / 4963 样本）。

## 3.9　wx-ct-fan-power-unit　—　逐台冷却塔风机功耗

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.1.0 |
| 状态 | 生产 |
| 对象模型 | ct_fan_unit.v1 |
| 建模对象 | CTF0101, CTF0102, CTF0103, CTF0201, CTF0202, CTF0203, CTF0301, CTF0302, CTF0303, CTF0401, CTF0402, CTF0403 |
| 预测目标 | power（kW） |
| 一句话说明 | 逐台冷却塔风机功耗（频率 + 湿空气密度修正 → 功耗），12 台 |
| 研究目标 | RG-0028 · 逐台冷却塔风机功耗模型（频率→功耗） |
| 假设 | H-0143：验证湿空气密度修正（ρ(T,RH)/ρ_ref 乘在频率幂律项上）能否降低滚动 CVRMSE 并压低夏季 NMBE 高估。 |
| 实验 / 视图 | EXP-0170 / VIEW-0071 |
| 数据集修订 | `WX_CT_FAN_UNIT@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | frequency | 本台变频器频率 | Hz | float | measured | 是 |
| 输入 | tower_fan_count | 同塔运行风机数 | 1 | float | measured | 是 |
| 输入 | tower_freq_mean | 同塔风机平均频率 | Hz | float | measured | 是 |
| 输入 | bank_fan_count | 全场运行风机数 | 1 | float | measured | 是 |
| 输入 | bank_freq_mean | 全场风机平均频率 | Hz | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | ambient_h | 室外相对湿度 | % | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：频率幂律 + 湿空气密度修正 —— 实验室模块 ct_fan_density_corrected_v1@v1

```
e_s(T)  = 610.94 · exp(17.625·T / (T + 243.04))       （饱和水汽压 Pa，Magnus 式）
p_v     = RH/100 · e_s(T)                             （水汽分压 Pa）
ρ(T,RH) = (p − p_v)/(287.058·(T+273.15))
          + p_v/(461.495·(T+273.15))                  （湿空气密度 kg/m³，p = 101325 Pa 定值）
P̂       = a · (f/50)^b · ρ(T,RH)/ρ_ref + c            （单台风机功率 kW）
```

**代入本模型辨识出的参数**：

```
P̂ = 39.3644 · (f/50)^2.7 · ρ(T,RH)/1.22492 + (-0.106334)
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| a | 50 Hz 基准功率系数 | 39.3644 | kW |
| b | 频率指数（相似定律理论值 3） | 2.7 | 1 |
| c | 常数偏置 | -0.106334 | kW |
| ρ_ref | 训练集湿空气密度均值 | 1.22492 | kg/m³ |
| b 搜索网格 | [b_min, b_max] × 步数 | [1.5, 4] × 201 | — |

> 拟合方式：b 在网格上枚举，每个 b 下对 (a, c) 做最小二乘，取 RMSE 最小者；全过程无随机数，`seed` 不影响结果。

> 辨识出的 b = 2.7 低于相似定律理论值 3 —— 变频器与电机效率随频率下降，实测幂次通常落在 2.5~2.8。

> **12 台风机共用一组 (a, b, c)**：模型不区分 object_id，逐台差异全部落进残差（见逐台指标表）。

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 208730（9 折，跳过 0） | 0.5774 | 6.08% | -0.22% | 4.82% |
| 验证集（时间切分中段） | 44768 | 0.9578 | 5.41% | 3.51% | 3.89% |
| 测试集 A（时间切分末段） | 44317 | 0.9867 | 4.51% | -0.83% | 3.42% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.5774 | 0.6272 | -0.9197 | 0.9883 | 9 |
| CVRMSE | 6.08% | 1.96% | 2.89% | 9.24% | 9 |
| NMBE | -0.22% | 2.73% | -6.26% | 3.34% | 9 |
| MAPE | 4.82% | 1.81% | 2.20% | 7.96% | 9 |
| RMSE | 1.681 | 0.9146 | 0.2907 | 2.913 | 9 |
| MAE | 1.34 | 0.7626 | 0.2166 | 2.407 | 9 |

**验证集（时间切分中段） 上的逐台拆分**

| 对象 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| CTF0101 | 4729 | 0.9625 | 5.26% | 3.91% | 3.67% |
| CTF0102 | 4727 | 0.975 | 4.22% | 2.28% | 2.90% |
| CTF0103 | 4726 | 0.9515 | 5.91% | 4.80% | 4.36% |
| CTF0201 | 1868 | 0.9775 | 3.88% | -2.64% | 3.32% |
| CTF0202 | 1868 | 0.937 | 5.90% | 5.08% | 4.73% |
| CTF0203 | 1868 | 0.9583 | 4.78% | -1.45% | 3.06% |
| CTF0301 | 4859 | 0.9693 | 4.61% | 2.25% | 3.14% |
| CTF0302 | 4858 | 0.957 | 5.52% | 4.50% | 4.16% |
| CTF0303 | 4858 | 0.9583 | 5.44% | 3.84% | 3.93% |
| CTF0401 | 3469 | 0.9089 | 7.48% | 6.51% | 6.03% |
| CTF0402 | 3469 | 0.9663 | 4.52% | 3.32% | 3.28% |
| CTF0403 | 3469 | 0.9452 | 5.63% | 4.17% | 4.28% |

**测试集 A（时间切分末段） 上的逐台拆分**

| 对象 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| CTF0101 | 3615 | 0.9792 | 4.30% | -1.59% | 3.06% |
| CTF0102 | 3623 | 0.969 | 5.25% | -3.51% | 4.64% |
| CTF0103 | 3626 | 0.9842 | 3.72% | -1.34% | 2.90% |
| CTF0201 | 3344 | 0.976 | 6.27% | -5.73% | 6.80% |
| CTF0202 | 3343 | 0.9893 | 4.06% | 1.41% | 2.72% |
| CTF0203 | 3326 | 0.991 | 3.75% | -2.19% | 3.71% |
| CTF0301 | 3621 | 0.9929 | 3.25% | 0.18% | 1.99% |
| CTF0302 | 3518 | 0.9867 | 4.52% | 1.59% | 2.67% |
| CTF0303 | 3520 | 0.9923 | 3.35% | 1.02% | 1.99% |
| CTF0401 | 4269 | 0.9847 | 5.16% | 0.77% | 3.18% |
| CTF0402 | 4259 | 0.9866 | 4.78% | -2.20% | 4.58% |
| CTF0403 | 4253 | 0.9885 | 4.24% | 0.62% | 2.95% |

缺失值丢弃行数：训练 68999、验证 14752、测试 15251（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0028 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 6.50% | 6.08% | 通过 |
| \|NMBE\| ≤ | 2.00% | 0.22% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=14, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-09-17 18:00 | 23162 |
| 验证 validate | 2025-09-17 18:45 → 2025-11-08 10:45 | 4960 |
| 测试 A | 2025-11-08 10:45 → 2025-12-30 21:45 | 4964 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `4d1dd661d2058271…`；物理检查违规率 0.00%（0 次 / 44317 样本）。

### 版本历史

| 版本 | 状态 | 实验 | 形态 | R²(滚动) | CVRMSE(滚动) | NMBE(滚动) | 说明 |
|---|---|---|---|---|---|---|---|
| 1.0.0 | 候选 | EXP-0170 | 频率幂律 + 湿空气密度修正 | 0.5774 | 6.08% | -0.22% | 逐台冷却塔风机功耗（频率+湿空气密度修正→功耗），12 台 |
| 1.1.0 | 生产（当前生产） | EXP-0170 | 频率幂律 + 湿空气密度修正 | 0.5774 | 6.08% | -0.22% | 逐台冷却塔风机功耗（频率 + 湿空气密度修正 → 功耗），12 台 |

## 3.10　wx-ct-supply-temp　—　冷却塔群出水温静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | ct_model.v1 |
| 建模对象 | CTBANK |
| 预测目标 | t_ct_out（Cel） |
| 一句话说明 | 冷却塔群出水温静态模型：ridge + 11 特征，rolling_cv 12 折 R2=0.978 / CVRMSE=1.27% / NMBE=+0.24% |
| 研究目标 | RG-0016 · 冷却塔群出水温模型（判据面=滚动交叉验证，R²≥0.90） |
| 假设 | H-0063：在 VIEW-0051 全候选输入上跑 ridge 基线，验证 t_ct_out 是否能达到 rolling_cv R²≥0.90 且 \|NMBE\|≤0.02；若复现已知的 R²≈0.978 即收工。 |
| 实验 / 视图 | EXP-0090 / VIEW-0051 |
| 数据集修订 | `WX_CT_MODEL@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | t_ct_in | 冷却塔群进水温 | Cel | float | measured | 是 |
| 输入 | f_cw_total | 冷却水总流量 | m3/h | float | measured | 是 |
| 输入 | t_wetbulb | 室外湿球温度 | Cel | float | estimated | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| 输入 | ambient_h | 室外相对湿度 | % | float | measured | 是 |
| 输入 | fan_run_count | 风机运行台数 | 1 | float | measured | 是 |
| 输入 | fan_freq_mean | 风机平均频率 | Hz | float | measured | 是 |
| 输入 | fan_freq_max | 风机最高频率 | Hz | float | measured | 是 |
| 输入 | cwp_run_count | 冷却泵运行台数 | 1 | float | measured | 是 |
| 输入 | chiller_run_count | 冷机运行台数 | 1 | float | measured | 是 |
| 输入 | fan_power_total | 风机群总功耗 | kW | float | measured | 是 |
| **输出** | t_ct_out | 冷却塔群出水温 | Cel | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：线性基线（岭回归 Ridge）

```
z_i = (x_i − μ_i) / σ_i        （训练集均值/标准差，随包落盘）
ŷ   = b + Σ_i w_i · z_i
```

**代入本模型辨识出的参数**：

```
ŷ = 20.1541
      + 4.20699 · z_t_ct_in
      + 0.94627 · z_f_cw_total
      + 0.367464 · z_t_wetbulb
      − 0.351815 · z_fan_freq_mean
      − 0.241477 · z_ambient_t
      + 0.151889 · z_fan_power_total
      − 0.0514277 · z_chiller_run_count
      − 0.0453839 · z_fan_run_count
      + 0.0256307 · z_ambient_h
      − 0.0217439 · z_cwp_run_count
      + 0.0185915 · z_fan_freq_max
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| b | 截距 | 20.1541 | 目标单位 |
| α | 岭回归正则强度 | 1 | 1 |
| w[t_ct_in] | 冷却塔群进水温 系数（作用在标准化量上） | 4.20699 | 目标单位 / 标准差 |
| μ,σ[t_ct_in] | 训练集均值 / 标准差 | 26.4275 / 4.60191 | 原始单位 |
| w[f_cw_total] | 冷却水总流量 系数（作用在标准化量上） | 0.94627 | 目标单位 / 标准差 |
| μ,σ[f_cw_total] | 训练集均值 / 标准差 | 2518.59 / 418.555 | 原始单位 |
| w[t_wetbulb] | 室外湿球温度 系数（作用在标准化量上） | 0.367464 | 目标单位 / 标准差 |
| μ,σ[t_wetbulb] | 训练集均值 / 标准差 | 9.30829 / 11.3466 | 原始单位 |
| w[fan_freq_mean] | 风机平均频率 系数（作用在标准化量上） | -0.351815 | 目标单位 / 标准差 |
| μ,σ[fan_freq_mean] | 训练集均值 / 标准差 | 41.9445 / 7.24011 | 原始单位 |
| w[ambient_t] | 室外干球温度 系数（作用在标准化量上） | -0.241477 | 目标单位 / 标准差 |
| μ,σ[ambient_t] | 训练集均值 / 标准差 | 14.2139 / 12.0732 | 原始单位 |
| w[fan_power_total] | 风机群总功耗 系数（作用在标准化量上） | 0.151889 | 目标单位 / 标准差 |
| μ,σ[fan_power_total] | 训练集均值 / 标准差 | 232.07 / 88.1026 | 原始单位 |
| w[chiller_run_count] | 冷机运行台数 系数（作用在标准化量上） | -0.0514277 | 目标单位 / 标准差 |
| μ,σ[chiller_run_count] | 训练集均值 / 标准差 | 1.55201 / 1.46936 | 原始单位 |
| w[fan_run_count] | 风机运行台数 系数（作用在标准化量上） | -0.0453839 | 目标单位 / 标准差 |
| μ,σ[fan_run_count] | 训练集均值 / 标准差 | 9.02366 / 0.480614 | 原始单位 |
| w[ambient_h] | 室外相对湿度 系数（作用在标准化量上） | 0.0256307 | 目标单位 / 标准差 |
| μ,σ[ambient_h] | 训练集均值 / 标准差 | 51.9568 / 24.2947 | 原始单位 |
| w[cwp_run_count] | 冷却泵运行台数 系数（作用在标准化量上） | -0.0217439 | 目标单位 / 标准差 |
| μ,σ[cwp_run_count] | 训练集均值 / 标准差 | 3.00125 / 0.0399527 | 原始单位 |
| w[fan_freq_max] | 风机最高频率 系数（作用在标准化量上） | 0.0185915 | 目标单位 / 标准差 |
| μ,σ[fan_freq_max] | 训练集均值 / 标准差 | 43.1013 / 6.55162 | 原始单位 |

> 系数作用在**标准化**特征上：|w| 可直接横向比大小，含义是「该输入每变化一个训练集标准差，输出变化多少」。

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 8027（12 折，跳过 0） | 0.9776 | 1.27% | 0.24% | 0.97% |
| 验证集（时间切分中段） | 4959 | 0.9579 | 2.85% | -2.27% | 2.31% |
| 测试集 A（时间切分末段） | 4963 | 0.3629 | 2.80% | -2.27% | 2.38% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.9776 | 0.02025 | 0.9175 | 0.9931 | 12 |
| CVRMSE | 1.27% | 0.73% | 0.53% | 2.76% | 12 |
| NMBE | 0.24% | 0.23% | -0.13% | 0.76% | 12 |
| MAPE | 0.97% | 0.54% | 0.43% | 2.07% | 12 |
| RMSE | 0.2722 | 0.1428 | 0.1403 | 0.596 | 12 |
| MAE | 0.204 | 0.09884 | 0.1121 | 0.4247 | 12 |

缺失值丢弃行数：训练 2、验证 1、测试 0（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0016 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| \|NMBE\| ≤ | 2.00% | 0.24% | 通过 |
| R² ≥ | 0.9 | 0.9776 | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（12 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=20, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-09-17 18:00 | 23159 |
| 验证 validate | 2025-09-17 18:45 → 2025-11-08 10:45 | 4960 |
| 测试 A | 2025-11-08 10:45 → 2025-12-30 21:15 | 4963 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `4878d97b0156db37…`；物理检查违规率 0.00%（0 次 / 4963 样本）。

## 3.11　wx-hx-heat-transfer　—　HXBANK 静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.1.0 |
| 状态 | 生产 |
| 对象模型 | hx_model.v1 |
| 建模对象 | HXBANK |
| 预测目标 | heat_transfer（kW） |
| 一句话说明 | HXBANK 静态模型：纯 GBDT gbdt_static@v1 + 9 入口特征，rolling_cv R2=0.759 / CVRMSE=1.82% / NMBE=+0.27% |
| 研究目标 | RG-0013 · 板换换热量静态模型（判据面=滚动交叉验证） |
| 假设 | H-0059：HXBANK：纯 GBDT（gbdt_static@v1，9 入口特征）在打开物理校验后指标不变，且预测满足硬物理约束，可进入发布。 |
| 实验 / 视图 | EXP-0086 / VIEW-0042 |
| 数据集修订 | `WX_HX_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | t_chw_in | 板换热侧（冷冻水）进水温 | Cel | float | measured | 是 |
| 输入 | t_cw_in | 板换冷侧（冷却水）进水温 | Cel | float | measured | 是 |
| 输入 | drive_dt | 冷热侧进水温差（驱动温差） | K | float | estimated | 是 |
| 输入 | f_chw | 冷冻水侧流量 | m3/h | float | measured | 是 |
| 输入 | f_cw | 冷却水侧流量 | m3/h | float | measured | 是 |
| 输入 | flow_ratio | 两侧流量比 | 1 | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | chiller_run_count | 冷机运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| **输出** | heat_transfer | 换热量 | kW | float | estimated | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (t_chw_in, t_cw_in, drive_dt, f_chw, f_cw, flow_ratio, run_count, chiller_run_count, ambient_t)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = t_chw_in
        x_2 = t_cw_in
        x_3 = drive_dt
        x_4 = f_chw
        x_5 = f_cw
        x_6 = flow_ratio
        x_7 = run_count
        x_8 = chiller_run_count
        x_9 = ambient_t
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| chiller_run_count | 冷机运行台数 | 84.7% |
| run_count | 运行台数 | 9.8% |
| drive_dt | 冷热侧进水温差（驱动温差） | 1.7% |
| flow_ratio | 两侧流量比 | 1.5% |
| f_cw | 冷却水侧流量 | 0.9% |
| t_cw_in | 板换冷侧（冷却水）进水温 | 0.6% |
| f_chw | 冷冻水侧流量 | 0.4% |
| ambient_t | 室外干球温度 | 0.3% |
| t_chw_in | 板换热侧（冷冻水）进水温 | 0.1% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3415（6 折，跳过 0） | 0.759 | 1.82% | 0.27% | 1.39% |
| 验证集（时间切分中段） | 2880 | 0.428 | 2.78% | 1.32% | 2.17% |
| 测试集 A（时间切分末段） | 2883 | 0.5887 | 2.10% | 1.31% | 1.66% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.759 | 0.05015 | 0.7 | 0.8296 | 6 |
| CVRMSE | 1.82% | 0.20% | 1.56% | 2.06% | 6 |
| NMBE | 0.27% | 0.41% | -0.29% | 0.99% | 6 |
| MAPE | 1.39% | 0.14% | 1.21% | 1.59% | 6 |

缺失值丢弃行数：训练 1、验证 0、测试 0（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0013 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 3.20% | 1.82% | 通过 |
| \|NMBE\| ≤ | 1.00% | 0.27% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=15, max_rel_error=0） |
| rollback_recorded | 通过 | 回滚目标: 1.0.0 |

回滚目标版本：`1.0.0`。

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-10-27 13:30 | 13451 |
| 验证 validate | 2025-10-27 14:15 → 2025-11-26 14:15 | 2880 |
| 测试 A | 2025-11-26 14:15 → 2025-12-26 15:30 | 2883 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `40b03827b6b91459…`；物理检查违规率 0.00%（0 次 / 2883 样本）。

### 版本历史

| 版本 | 状态 | 实验 | 形态 | R²(滚动) | CVRMSE(滚动) | NMBE(滚动) | 说明 |
|---|---|---|---|---|---|---|---|
| 1.0.0 | 已弃用 | EXP-0052 | ε-NTU 逆流换热器模型 | 0.3821 | 2.61% | -0.69% | HXBANK 静态模型（判据面 rolling_cv） |
| 1.1.0 | 生产（当前生产） | EXP-0086 | 纯梯度提升静态模型 | 0.759 | 1.82% | 0.27% | HXBANK 静态模型：纯 GBDT gbdt_static@v1 + 9 入口 |

## 3.12　wx-hx-heat-transfer-static　—　板换换热量静态模型

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | hx_model.v1 |
| 建模对象 | HXBANK |
| 预测目标 | heat_transfer（kW） |
| 一句话说明 | 板换换热量静态模型（只用入口值）；EXP-0061 的干净重跑，原实验受实验室目标列泄漏影响，指标不可用 |
| 研究目标 | RG-0013 · 板换换热量静态模型（判据面=滚动交叉验证） |
| 假设 | H-0040：HXBANK：去掉物理主干、直接用梯度提升在白名单特征上回归（实验室模块 gbdt_static@v1），应优于 hybrid —— 蒸发侧温差摆幅仅 0.4 K 使热阻不可辨识（手册 §7.11），主干只贡献近似常数偏移，残差学习器还得先学回来。 |
| 实验 / 视图 | EXP-0177 / VIEW-0042 |
| 数据集修订 | `WX_HX_MODEL@rev_0003` |
| 随机种子 / 代码版本 | 20260817 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | t_chw_in | 板换热侧（冷冻水）进水温 | Cel | float | measured | 是 |
| 输入 | t_cw_in | 板换冷侧（冷却水）进水温 | Cel | float | measured | 是 |
| 输入 | drive_dt | 冷热侧进水温差（驱动温差） | K | float | estimated | 是 |
| 输入 | f_chw | 冷冻水侧流量 | m3/h | float | measured | 是 |
| 输入 | f_cw | 冷却水侧流量 | m3/h | float | measured | 是 |
| 输入 | flow_ratio | 两侧流量比 | 1 | float | estimated | 是 |
| 输入 | run_count | 运行台数 | 1 | float | measured | 是 |
| 输入 | chiller_run_count | 冷机运行台数 | 1 | float | measured | 是 |
| 输入 | ambient_t | 室外干球温度 | Cel | float | measured | 是 |
| **输出** | heat_transfer | 换热量 | kW | float | estimated | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：纯梯度提升静态模型（实验室模块 gbdt_static@v1，无物理主干）

```
ŷ = Σ_(t=1..600) g_t(x)        （XGBoost 回归树加和，目标函数 squarederror）
x = (t_chw_in, t_cw_in, drive_dt, f_chw, f_cw, flow_ratio, run_count, chiller_run_count, ambient_t)
```

**代入本模型辨识出的参数**：

```
输入向量按训练时列序取列：
        x_1 = t_chw_in
        x_2 = t_cw_in
        x_3 = drive_dt
        x_4 = f_chw
        x_5 = f_cw
        x_6 = flow_ratio
        x_7 = run_count
        x_8 = chiller_run_count
        x_9 = ambient_t
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| colsample_bytree |  | 0.9 | 1 |
| learning_rate |  | 0.05 | 1 |
| max_depth |  | 6 | 1 |
| min_child_weight |  | 1 | 1 |
| n_estimators |  | 600 | 1 |
| reg_alpha |  | 0 | 1 |
| reg_lambda |  | 1 | 1 |
| subsample |  | 0.9 | 1 |
| seed | 随机种子 | 20260817 | 1 |
| tree_method | 建树算法 | hist | — |
| n_jobs | 线程数（位级复现要求单线程） | 1 | 1 |

> 树集成没有闭式解析式：模型本体是 `artifact/booster.json` 里的 600 棵树，可解释性由下方**特征增益占比**给出。

> 确定性：单线程 + 固定 random_state + tree_method=hist，同种子重训逐位一致。

**特征增益占比（XGBoost gain，归一化到 100%）**

| 特征 | 含义 | 增益占比 |
|---|---|---|
| chiller_run_count | 冷机运行台数 | 84.7% |
| run_count | 运行台数 | 9.8% |
| drive_dt | 冷热侧进水温差（驱动温差） | 1.7% |
| flow_ratio | 两侧流量比 | 1.5% |
| f_cw | 冷却水侧流量 | 0.9% |
| t_cw_in | 板换冷侧（冷却水）进水温 | 0.6% |
| f_chw | 冷冻水侧流量 | 0.4% |
| ambient_t | 室外干球温度 | 0.3% |
| t_chw_in | 板换热侧（冷冻水）进水温 | 0.1% |

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 3415（6 折，跳过 0） | 0.759 | 1.82% | 0.27% | 1.39% |
| 验证集（时间切分中段） | 2880 | 0.428 | 2.78% | 1.32% | 2.17% |
| 测试集 A（时间切分末段） | 2883 | 0.5887 | 2.10% | 1.31% | 1.66% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.759 | 0.05015 | 0.7 | 0.8296 | 6 |
| CVRMSE | 1.82% | 0.20% | 1.56% | 2.06% | 6 |
| NMBE | 0.27% | 0.41% | -0.29% | 0.99% | 6 |
| MAPE | 1.39% | 0.14% | 1.21% | 1.59% | 6 |

缺失值丢弃行数：训练 1、验证 0、测试 0（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0013 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 3.20% | 1.82% | 通过 |
| \|NMBE\| ≤ | 1.00% | 0.27% | 通过 |
| 物理违规率 ≤ | 1.00% | 0.00% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（15 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，5 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=15, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-10-27 13:30 | 13451 |
| 验证 validate | 2025-10-27 14:15 → 2025-11-26 14:15 | 2880 |
| 测试 A | 2025-11-26 14:15 → 2025-12-26 15:30 | 2883 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `40b03827b6b91459…`；物理检查违规率 0.00%（0 次 / 2883 样本）。

## 3.13　wx-pump-power-unit　—　逐台水泵功耗

### 定位

| 项 | 值 |
|---|---|
| 版本 | 1.0.0 |
| 状态 | 生产 |
| 对象模型 | pump_unit.v1 |
| 建模对象 | CHWP01, CHWP02, CHWP03, CHWP04, CWP01, CWP02, CWP03, CWP04 |
| 预测目标 | power（kW） |
| 一句话说明 | 逐台水泵功耗（频率→功耗，按 object_id 逐台标定相似定律 P=a(f/50)^b+c），8 台 |
| 研究目标 | RG-0023 · 逐台水泵功耗模型（频率→功耗，相似定律 P∝f³） |
| 假设 | H-0151：验证按 object_id 逐台标定的三参数幂律模型 P=a(f/50)^b+c，在 rolling_cv 判据面上能否把逐台水泵功耗的 CVRMSE 压到 ≤3% 且 \|NMBE\|≤2%。 |
| 实验 / 视图 | EXP-0180 / VIEW-0066 |
| 数据集修订 | `WX_PUMP_UNIT@rev_0001` |
| 随机种子 / 代码版本 | 20260808 / `c2134bc2` |
| 环境锁 | `6def9db6a88c3bc4…` |

### 输入 → 输出

| 方向 | property_code | 含义 | 单位 | 类型 | 数据来源 | 必填 |
|---|---|---|---|---|---|---|
| 输入 | frequency | 本台变频器频率 | Hz | float | measured | 是 |
| **输出** | power | 功率 | kW | float | measured | — |

越界处理：全部输入 `out_of_range = reject`（超出训练包络直接拒绝，不外推）。

### 公式

**模型形态**：逐台标定的三参数相似定律幂律 —— 实验室模块 pump_power_per_object_affinity_v1@v1

```
P̂(obj, f) = 0                                 若 f ≤ 1e−9（停机判定）
P̂(obj, f) = c_obj + a_obj · (f/50)^b_obj       否则

每台独立标定：b_obj 在 [2.0, 3.5] 上 61 点网格枚举，
每个 b 下对 (a, c) 做最小二乘，取 RMSE 最小者；
样本数 < 5 的对象回退到全体样本标定的 global 参数。
```

**代入本模型辨识出的参数**：

```
CHWP01：P̂ = -7.83773 + 230.324 · (f/50)^2.75
CHWP02：P̂ = -10.7592 + 231.997 · (f/50)^2.675
CHWP03：P̂ = -13.9953 + 235.06 · (f/50)^2.575
CHWP04：P̂ = -14.5507 + 230.827 · (f/50)^2.6
CWP01：P̂ = 1.18103 + 187.311 · (f/50)^3.1
CWP02：P̂ = 0.424573 + 190.181 · (f/50)^3.075
CWP03：P̂ = 0.173853 + 192.283 · (f/50)^3
CWP04：P̂ = 1.7449 + 187.609 · (f/50)^3.125
```

**参数取值**

| 参数 | 含义 | 取值 | 单位 |
|---|---|---|---|
| CHWP01 | a（kW） / b（1） / c（kW） | 230.324 / 2.75 / -7.83773 | — |
| CHWP02 | a（kW） / b（1） / c（kW） | 231.997 / 2.675 / -10.7592 | — |
| CHWP03 | a（kW） / b（1） / c（kW） | 235.06 / 2.575 / -13.9953 | — |
| CHWP04 | a（kW） / b（1） / c（kW） | 230.827 / 2.6 / -14.5507 | — |
| CWP01 | a（kW） / b（1） / c（kW） | 187.311 / 3.1 / 1.18103 | — |
| CWP02 | a（kW） / b（1） / c（kW） | 190.181 / 3.075 / 0.424573 | — |
| CWP03 | a（kW） / b（1） / c（kW） | 192.283 / 3 / 0.173853 | — |
| CWP04 | a（kW） / b（1） / c（kW） | 187.609 / 3.125 / 1.7449 | — |
| global（兜底） | 样本不足对象的回退参数 a / b / c | 237.029 / 2 / -43.8082 | — |

> 冷冻泵 CHWP 标定出的 b ≈ 2.58~2.75，冷却泵 CWP b ≈ 3.00~3.13 —— 冷却泵更贴近相似定律的立方律，冷冻泵偏低。

> 拟合无随机数；`f ≤ 1e−9 → P̂ = 0` 这条硬规则保证停机时刻不产生虚假功耗。

> **可外推**：结构是频率幂律，给定任意开机组合与频率即可加总出总功耗。

### 结果

| 判据面 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| 滚动交叉验证（判据面） | 137735（9 折，跳过 0） | 0.9898 | 1.86% | 0.15% | 0.99% |
| 验证集（时间切分中段） | 29772 | 0.9987 | 1.46% | -0.38% | 1.07% |
| 测试集 A（时间切分末段） | 28632 | 0.9684 | 4.74% | 0.95% | 1.43% |

滚动交叉验证的折间统计（RMSE / MAE 为绝对量纲，单位同预测目标）：

| 指标 | 折间均值 | 折间标准差 | 最小 | 最大 | 有效折数 |
|---|---|---|---|---|---|
| R2 | 0.9898 | 0.01656 | 0.9434 | 0.9984 | 9 |
| CVRMSE | 1.86% | 1.42% | 0.73% | 5.60% | 9 |
| NMBE | 0.15% | 0.52% | -0.64% | 1.27% | 9 |
| MAPE | 0.99% | 0.34% | 0.55% | 1.78% | 9 |
| RMSE | 1.492 | 0.8211 | 0.378 | 3.575 | 9 |
| MAE | 0.8257 | 0.2487 | 0.2882 | 1.113 | 9 |

**验证集（时间切分中段） 上的逐台拆分**

| 对象 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| CHWP01 | 4822 | 0.9986 | 1.20% | -0.25% | 0.88% |
| CHWP02 | 1605 | 0.9949 | 1.47% | 0.37% | 0.98% |
| CHWP03 | 4959 | 0.9992 | 0.94% | 0.02% | 0.71% |
| CHWP04 | 3500 | 0.9968 | 1.28% | -0.34% | 0.96% |
| CWP01 | 4822 | 0.9946 | 1.91% | -0.93% | 1.52% |
| CWP02 | 1606 | 0.9939 | 1.68% | 0.24% | 0.67% |
| CWP03 | 4958 | 0.9972 | 1.56% | -0.59% | 1.04% |
| CWP04 | 3500 | 0.9929 | 2.42% | -1.17% | 1.60% |

**测试集 A（时间切分末段） 上的逐台拆分**

| 对象 | 样本数 | R² | CVRMSE | NMBE | MAPE |
|---|---|---|---|---|---|
| CHWP01 | 3330 | 0.9855 | 1.14% | 0.31% | 0.68% |
| CHWP02 | 3343 | 0.9776 | 1.73% | 0.59% | 0.85% |
| CHWP03 | 3258 | 0.9945 | 0.92% | 0.44% | 0.70% |
| CHWP04 | 3934 | 0.9884 | 1.42% | 0.18% | 0.74% |
| CWP01 | 3659 | 0.9963 | 1.21% | -0.28% | 0.64% |
| CWP02 | 3340 | 0.2099 | 15.86% | 6.94% | 6.27% |
| CWP03 | 3491 | 0.9583 | 3.82% | 0.60% | 1.02% |
| CWP04 | 4277 | 0.9717 | 3.52% | 0.47% | 0.91% |

缺失值丢弃行数：训练 46273、验证 9916、测试 11088（该行任一输入或目标为空即整行丢弃）。

### 验收与门禁

目标 RG-0023 声明的判据面：`rolling_cv`。

| 判据 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| CVRMSE ≤ | 3.00% | 1.86% | 通过 |
| \|NMBE\| ≤ | 2.00% | 0.15% | 通过 |

发布门禁（`runtime/registry.py`，模型包 §8）：

| 门禁 | 结论 | 说明 |
|---|---|---|
| integrity | 通过 | 文件齐全且校验和一致（14 个文件） |
| signature_tfom | 通过 | 未提供 TFOM 注册表，跳过兼容检查 |
| acceptance | 通过 | 硬性验收条件全部满足（判据面 rolling_cv，7 项指标） |
| latency | 通过 | 未声明延迟上限，跳过 |
| smoke | 通过 | 冷加载 + golden 比对通过（n=3, max_rel_error=0） |
| rollback_recorded | 通过 | 首个生产版本，无回滚目标（已记录） |

### 数据与时间切分

| 切分段 | 时间区间（UTC） | 行数 |
|---|---|---|
| 训练 train | 2024-12-31 16:00 → 2025-09-17 18:45 | 23165 |
| 验证 validate | 2025-09-17 19:30 → 2025-11-08 11:45 | 4961 |
| 测试 A | 2025-11-08 11:45 → 2025-12-30 23:45 | 4965 |
| purge + embargo 丢弃 | purge 0 s / embargo 2700 s | 3 |

视图哈希 `3b7f0515f664d370…`；物理检查违规率 0.00%（0 次 / 28632 样本）。

## 四、需要注意的地方

1. **`wx-ct-approach` 的包描述与实现不符。** 描述写「Merkel/NTU 主干 + 偏差校正」，落盘的 `lab_source.py` 里没有 Merkel 主干，实际是 21 项二阶多项式岭回归。描述沿用了假设 H-0099 的措辞，没随实现更新。以本报告的公式一节为准。

2. **两个 `-static` 模型是泄漏事故的干净重跑。** `wx-chiller-power-static`（EXP-0176，重跑 EXP-0058）与 `wx-hx-heat-transfer-static`（EXP-0177，重跑 EXP-0061）对应的原始实验受实验室目标列泄漏影响，原指标不可用；这里的指标来自重跑。

3. **`wx-hx-heat-transfer` 1.1.0 与 `wx-hx-heat-transfer-static` 1.0.0 指标完全相同。** 两者同视图 VIEW-0042、同模块、同超参、同种子，是同一个模型的两次注册（前者带物理校验重跑，后者是泄漏事故的干净重跑），不是两个独立结果。

4. **`wx-chwp-flow-total` 用 `power_total` 预测 `flow_total`。** 增益占比里 power_total 一项占 61.8%——这是「用泵功耗反推流量」，物理上成立（同一台泵上功耗与流量单调相关），但它要求功耗测点在线可用，做流量**软测量**没问题，做 what-if（改频率看流量）时要小心：功耗本身也是频率的函数。

5. **`wx-chiller-power-ch0x` 的 Gordon-Ng 主干普遍退化。** CH01/CH03/CH04 辨识出的换热热阻里至少有一个被压到 1e−12 及更小（CH04 是 R_e、R_c 双双归零），物理主干几乎只剩熵产项。原因是蒸发侧温差摆幅只有 0.4 K 量级，热阻不可辨识。CH02 因此改走纯 GBDT 路线（假设 H-0037），指标从 R²=0.588 提到 0.723。

6. **逐台风机模型 12 台共用一组参数。** `wx-ct-fan-power-unit` 不区分 object_id，逐台差异全部进残差；逐台指标表里 CTF0401 的验证面 R²=0.909 是最差的一台。误差两两相关系数均值 +0.314（F-0087），说明存在共模偏差——继续压随机误差对群口径收益有限。

7. **四台冷机的缺失行丢弃都不小，CH02 尤其极端。** 训练段共约 10450 行，CH01 丢 3420、CH03 丢 3515、CH02 丢 2766、CH04 丢 2151（机组停机时该行整行为空）。更要紧的是 CH02 的测试段：2239 行里丢掉 2233 行，**测试面 A 只剩 6 个样本**，那一列的 R²=−15.97 之类的数字没有统计意义。看 A 面指标一定要先看样本数。

8. **`RG-0012` 的 NMBE 上限被人工放宽过。** 2026-08-21 由 2.0% 改到 3.5%，为的是放行 CH01（2.47%）与 CH03（3.40%）。这是需求方的显式决定，记在目标的 transitions 里，不是模型指标变好了。

---

报告由 `scripts/export_model_report.py` 生成；数据源为本机 `models/` 与 `research/` 目录的当前内容。重新生成：

```
.venv/Scripts/python scripts/export_model_report.py
```

