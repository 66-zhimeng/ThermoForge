# ThermoForge 文档

ThermoForge 是一个面向数据中心 HVAC 的自主物理—数据混合建模研究系统。

> 给 ThermoForge 物模型、历史数据和研究目标，让它持续提出假设、执行实验、积累证据并产出可部署模型。

英文定位：**Autonomous Physics-Data Hybrid Modeling Research System**。

## 核心原则

1. Pi/Agent 负责研究推理、流程编排和工具调用，不直接承担大规模科学计算。
2. Python 计算运行时负责数据处理、参数辨识、训练、仿真、优化和验证。
3. Excel 是数据交换入口，不是系统内部数据库；导入后转换为可复用、可追溯的数据资产。
4. 物模型、Excel、实验、模型签名、API 和现场绑定使用同一套变量语义。
5. 研究过程必须持久化。目标、假设、实验、证据、结论和失败原因不能只存在于 LLM 上下文中。
6. 模型评价不仅关注拟合精度，还必须覆盖泛化能力、物理一致性、稳定性、复杂度和运行成本。
7. 原始数据、数据版本和已完成实验不可被原地覆盖，确保结果可复现。

## 文档导航

- [系统总体设计](./architecture.md)：系统边界、组件、Agent 职责和技术分层。
- [TFDC 数据契约](./data-contract.md)：统一变量体系、Excel 规范、Data Vault 和数据版本。
- [自主研究闭环](./research-loop.md)：研究状态机、实验协议、验证与模型评分。
- [模型包与部署契约](./model-package.md)：模型签名、制品结构、注册和线上接入。
- [实施路线图](./roadmap.md)：MVP 范围、阶段划分、验收标准与后续演进。

## 关键契约

| 契约 | 职责 |
|---|---|
| TFOM | 定义对象、属性、单位、角色、派生关系和物理约束 |
| TFDC | 定义历史与实时数据如何表达和交换 |
| Dataset View | 定义可复用、可哈希的数据选择与预处理结果 |
| Research Goal | 定义研究对象、目标变量、候选输入和验收标准 |
| Experiment | 定义一次可复现的模型实验 |
| Model Package | 定义可验证、可部署的模型交付物 |

## 一期建议

第一阶段不要从 UI 或复杂多 Agent 系统开始。优先打通以下最小闭环：

```text
TFOM + TFDC-XLSX
        ↓
导入、校验、版本化、数据画像
        ↓
Research Goal
        ↓
基线模型 + 一个物理模型 + 一个混合模型
        ↓
时序验证 + 物理约束验证
        ↓
Research Ledger + Model Package
```

